小小思考
Redux 应该只负责管理共用的状态，比如用户信息，登录信息以及一些数据层面上的状态，例如时间、天气、产品列表。。。

而针对一些 UI 级别的状态变化，应该放在组件私有 state，这个类别有：

UI 层面的 toggle 状态，展示全部/部分，加载更多

为了实现动画而维护的状态值，比如拖拽改变其组件的坐标值

在这个问题中，每块小的组件都有自己的状态，那么就把这部分状态放在组件私有属性state；它们公共的状态将放在Redux中。
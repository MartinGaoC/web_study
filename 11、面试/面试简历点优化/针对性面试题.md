# Vue 和 React的区别

* 设计思想
    vue： 一款渐进式框架，主要由数据进行驱动
    React： 函数式编程框架，推出纯组件，数据不可变，单项数据流
* 编写语法
    Vue： hteml css js 分开
    React： 全部构建为JSX 语法
* 数据绑定
    Vue: MVVM 数据双向绑定，通过 object defineProperty 劫持了 数据的 getter 和set方法
    React： 单项数据流，数据从上而下。react数据不允许直接修改，需要通过setState的方法进行修改
* 渲染方式
    React: 通过JSX的方式来渲染模版，本质上React式通过原生JS的形式来实现模版中的常用语法
    Vue: 而Vue则是在和组件JS代码分离的模版中通过指令来实现的
 一般大型项目 React的健壮性会更好

 小项目 使用Vue会更加灵活

 # 立即获取setState改变后的值
  - 类组件中 使用this.setState的第二个参数回调函数来获取
  - 函数组件中可以使用useEffect钩子函数



# 垃圾回收机制
  - v8回收机制采用了新生代结合老生代的垃圾回收方式
  - 把内存分为新生代和老生代
  - 新生代频繁进行检查， 新生代分为 from 和to  数据状态先放到from里  如果检查存活  则复制到to里 如此循环
  - 老生代大部分保留存活时间比较长的对象
  - 对象晋升  老生代的对象都是新生代晋升而来  to的占用空间超过百分之25  经过过第一次垃圾回收检查 到第二次的时候 就会复制到老生代里

# 堆和栈
 - 堆栈其实是两种数据结构，
    - 堆存储的大小空间不固定，是由程序员自己申请的
    - 栈的存储大小是固定的，一般是连续存储，由系统进行控制